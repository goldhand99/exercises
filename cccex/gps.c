
#include <stdio.h>
#include <stdlib.h>
#define MAXLINE 9892

int min_distance(int **visited_sets, int **distance_source)
{
}

int main()
{

    int visited_sets[MAXLINE];
    int distance_source[MAXLINE];

    for (int i = 0; i < 60; i++)
    {
        visited_sets[i] = 0;
        distance_source[i] = INT32_MAX;
    }

    char matrix[6][6] = {
        [1],
        [ 2, 7, 10 ],
        [ 3, 5, 4 ],
        []

    };
}
